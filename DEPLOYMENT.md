# 公開と運用

## 必要な構成

Reactのビルド成果物とNode.js APIを同じHTTPSオリジンで公開します。Node.js 24.14以上の24系、永続ディスク、プロセス再起動機能が必要です。SQLiteは単一インスタンスで運用します。自動で破棄されるサーバーレスのファイル領域や複数レプリカでは使わないでください。

現在のGitHub Pages設定は静的画面のみを配信します。マージだけではログイン・保存機能は本番で有効になりません。Node対応サーバーの契約・接続情報が決まったら、以下の構成を適用できます。

## 起動

1. `npm ci` → `npm test` → `npm run build`。
2. 環境変数を設定: `NODE_ENV=production`、`APP_ORIGIN=https://公開ドメイン`、`DATABASE_PATH=/永続領域/gyosai.sqlite`。必要なら `HOST=0.0.0.0` と `PORT=8787`。
3. `npm start` をプロセス管理下で起動し、HTTPSリバースプロキシから転送します。`APP_ORIGIN` はブラウザーから見えるオリジンと完全一致させてください。
4. `/api/session` と `/api/materials` が200を返すことを確認。
5. 自分のアカウントを画面で登録し、サーバーで `npm run admin -- メールアドレス` を実行。再読み込み後、管理画面が表示されます。DBパスを同じ環境変数にしてください。
6. 別の一般アカウントでレビュー投稿→管理者承認→公開を確認。

Dockerの場合は同梱Dockerfileを使い、`/data` に永続ボリューム、`APP_ORIGIN` にHTTPSの公開オリジンを設定します。コンテナー外でTLSを終端してください。Docker環境自体は今回のローカル検証対象外です。

## AI補足

`OPENAI_API_KEY` と利用可能な `OPENAI_MODEL` をサーバー環境に設定します。キーを `VITE_` 変数、ブラウザー、Gitに入れないでください。AI補足は1ユーザー1時間5回までです。キー未設定では条件診断のみ利用でき、失敗時にAI出力を捏造しません。モデルごとの利用可否・費用は利用するアカウントで確認してください。

参考: https://developers.openai.com/api/reference/cli/resources/responses/methods/create

## 保存・バックアップ

`npm run backup` で `data/backups/` に一貫性のあるSQLiteスナップショットを作成できます。引数でバックアップ先を指定できます。バックアップにはアカウント・非公開記録が含まれるため、公開ディレクトリに置かずアクセスを制限してください。

復元時はサーバーを停止し、現DBを別の保存先へ退避してからバックアップを `DATABASE_PATH` に復元します。WAL・SHMファイルを含む稼働DBの単純コピーをバックアップとして使わないでください。

## 制約

- 初期掲載は公式出典を確認した模試3件。管理画面で答練・通信講座・過去問等も追加できます。料金・品質評価は推測で埋めません。
- メール認証・パスワード再発行メールは未搭載です。一般公開前にメールサービスを選んで接続するか、登録対象を限定して運用してください。
- 書き込みには許可済みOriginと専用ヘッダーが必要。認証・AI・一般書き込みの回数制限はDBに保持。プロキシ経由のIPは意図的に無条件には信頼していないため、1プロキシ配下ではIP制限が共有されます。
- 管理変更とレビュー公開操作は監査履歴に残ります。広告クリックは日付・教材単位で保存し、IPやユーザーIDを保存しません。
